# Render Blueprint para MitaBot Cartola
# Deploy automático de todos os serviços

services:
  # Redis
  - type: redis
    name: mitabot-redis
    plan: free
    ipAllowList: []

  # Frontend Next.js
  - type: web
    name: mitabot-frontend
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install && npx prisma generate && npm run build
    startCommand: cd frontend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: mitabot-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: mitabot-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: ML_SERVICE_URL
        value: http://mitabot-ml-service

  # ML Service (Python)
  - type: web
    name: mitabot-ml-service
    runtime: python
    plan: free
    buildCommand: cd ml-service && pip install -r requirements.txt
    startCommand: cd ml-service && uvicorn src.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mitabot-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: mitabot-redis
          property: connectionString
      - key: MODEL_PATH
        value: /tmp/models

  # Workers
  - type: worker
    name: mitabot-workers
    runtime: node
    plan: free
    buildCommand: cd workers && npm install
    startCommand: cd workers && npm run start:workers
    envVars:
      - key: REDIS_URL
        fromService:
          type: redis
          name: mitabot-redis
          property: connectionString
      - key: FRONTEND_URL
        value: https://mitabot-frontend.onrender.com
      - key: ML_SERVICE_URL
        value: http://mitabot-ml-service

databases:
  - name: mitabot-db
    databaseName: mitabot
    user: mitabot
    plan: free
